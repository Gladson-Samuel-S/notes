import { Link } from "react-router-dom";

const CreateNote = () => {

    return (
        <div className="CreateNote-container">
            <Link to={'/'}>Back</Link>
        </div>
    )
}

export default CreateNote;